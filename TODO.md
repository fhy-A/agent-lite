# Code TODO  _上次更新：2026-07-22_

> 只记录尚未完成且可以继续执行的事项；已完成内容统一查阅 `CHANGELOG.md`。

## 已知问题

- [ ] **流式思考块首帧闪现左侧** — 发送消息后模型思考内容首帧出现在消息区左边缘，随即跳回居中位置。疑似浏览器 flex 布局 bug（Mozilla #1343370：flex 新子元素首帧使用容器旧尺寸做对齐）。已尝试：display:contents 移除、flex align-items:center、text-align:center+inline-block、left:50%+translateX、visibility:hidden+rAF 延迟显示、rAF 延迟插入、scrollbar-gutter:stable、contain:layout 等方案均未根治。下一轮调试方向：排查是否来自 messageList 之外的 DOM 操作（activeRunBanner 移动等）、或用 PerformanceObserver 录一帧确认首帧绘制来源。
- [x] **自动模式下工具执行失败后卡"等待批准"** — 已修复：`authorizationDecision === "approved"` 的 server-managed 编辑直接视为已应用，不再卡在等待批准。模型自行重试。
- [ ] **子 Agent 接管队列消息时缺乏上下文判断** — 主 Agent 正在流式输出时发送的新消息会通过 `dispatchBackgroundSubAgent` 交给子 Agent 处理。但子 Agent 得到的上下文是精简的（只有当前会话摘要），难以判断该任务是应该排队等主 Agent 还是自己独立处理，容易误执行本应由主 Agent 完成的操作。需考虑：① 子 Agent 是否能感知主 Agent 的当前任务状态；② 是否需要一个"消息分类器"先判断消息类型再决定路由；③ 或者所有队列消息都先等待主 Agent 空闲后再交给它。
- [x] **会话中模型错误后无法恢复** — 已修复：非 AbortError 自动回滚到本轮前的健康快照 + 清理残留 streaming 标记 + 追加恢复提示。`/branch` 兜底建议已写入错误消息。

## P0 · 上线前

- [ ] **中转站品牌 logo 设计** — 为 Linkwork 设计品牌 logo，用于网站、支付页面、邮件模板等。
- [ ] **中转站相关文档** — 用户使用指南、API 对接文档、常见问题 FAQ。
- [ ] **中转站首页内容调整** — 当前为 New API 默认模板，需改为 Linkwork 品牌宣传内容（Hero、特性、CTA）。
- [ ] **准备上线资料** — 确认服务器、域名、HTTPS、备份、监控、文档与合规要求。
- [ ] **接入充值系统** — 已对接支付FM，待完成：找人扫码实测 ngrok 回调闭环、支付FM 商户名称改为品牌名。
- [ ] **API 号源选品与定价** — 调研竞品号源渠道，筛选性价比高的上游供应商，制定 Linkwork 定价体系（模型倍率、充值档位、汇率）。
- [ ] **获取营业执照** — 申请个体工商户营业执照，开通支付宝/微信官方商户。
- [ ] **支付渠道升级** — 拿执照后切签约模式（去浮动）或直接支付宝 API（复用 Mock 支付页面）。

## 待优化 · 错误恢复

- [ ] **智能错误分类重试（方案 C）** — 当前 A+B 已实现：非 AbortError 统一回滚到快照 + 兜底提示。进一步优化方向：① 按错误类型分类（4xx 客户端错误 vs 5xx 服务端错误 vs 网络超时 vs 内容过滤）；② 不同类别走不同恢复策略（4xx 回滚+提示修改、5xx/超时 自动重试 1-2 次、内容过滤 保留上下文但警告用户）；③ 错误统计面板（展示每类错误的频率和趋势）。

## P1 · 核心能力

- [ ] **补充 Skill 输出证据校验** — 对性能类方案中的未测量数字、阈值和性能排序增加结构化检查，不再只依赖提示词约束。
- [ ] **继续 `app.js` 拆分阶段 4** — 下一批盘点并抽离会话状态、持久化与分支领域，保持前台查看会话和后台运行会话相互隔离。
- [ ] **继续设置页分阶段优化** — 下一阶段重构主题模式与主题方案选择器；之后统一语言、账户、更新等轻量页面的卡片与按钮布局。
- [x] **收口剩余 i18n** — 合并 `src/core/i18n.js` 内 `LANG` / `I18N` 的重复键，并评估服务端错误字典是否采用统一键约定。→ 已完成：LANG 瘦身 68%（22 个被 I18N 覆盖的死键移除），I18N 移除 6 个零引用的 `*Label` 死键。`srv*` 错误键已统一前缀，当前映射方式（中文→key）可用但后续建议 server 直返 key。

## P2 · 后续路线

- [x] **code 多主题支持（内置主题）** — 15 套亮色 + 26 套暗色主题，带色板预览的变体选择器，`data-theme-mode`/`data-theme-variant` 标记。→ 高对比度主题和用户自定义主题待后续。
- [ ] **Agent 规划任务能力** — 支持复杂任务自动拆解、依赖排序和分阶段执行。
- [ ] **中转站深度整合** — 打通 Token 用量统计、统一模型选择器和 Key 管理闭环。
- [ ] **macOS / Linux 支持** — 适配 shell、进程管理、托盘和安装流程。
- [ ] **客户端应用** — 评估并开发 Windows / macOS 原生客户端。
- [ ] **营销获客** — 规划推广渠道、内容与 SEO。
