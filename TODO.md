# Agent Lite TODO

## 功能待办

- [ ] **Token 用量优化** — 降低 API 使用成本
  - 系统提示词精简（当前 SYSTEM_SECURITY_LAYER + 多段提示较长）
  - 工具结果截断（避免返回全量文件内容）
  - 上下文压缩策略优化（当前 95% 阈值触发 compact）
  - 流式输出 token 计数校准
  - 图片 base64 编码优化（压缩/缩略图）
  - 历史消息裁剪策略
- [ ] **app.js 模块化** — 399KB 单文件拆分（state / agent-loop / tools / render / events / session / utils），esbuild 打包
- [ ] **macOS / Linux 跨平台** — PowerShell→bash/zsh、进程管理、托盘适配
- [ ] **API 中转站部署**（阶段 2）— 共享 auth、Key 配置、嵌入中转站页面
- [x] **网络异常重试机制** — 网络断开、超时和 SSE 半途断开时自动等待并进行有上限的退避重试
- [ ] **后台任务 + 刷新不中断**
  - [x] 任意会话有任务执行时仍可新建/切换会话，不阻塞 UI
  - [x] 刷新后从持久化检查点自动续跑，并对可能产生副作用的工具阶段先核查再执行
  - [ ] Agent 循环下沉到 server 端，刷新后续接原始任务事件流而非重新建立模型请求
- [ ] **深度整合**（阶段 3）— Token 用量统计、统一模型选择器、Key 管理闭环

## 对话问卷待办

- [ ] **关键抉择问卷** — 仅在缺少用户指引会显著影响任务目标或方案时触发
  - 触发时机：复杂任务开始阶段需求仍不明确，或执行中遇到无法可靠推断的关键抉择；普通选择继续由 Agent 根据上下文自主判断
  - 展示位置：作为与任务请求同级的交互卡片，固定显示在对话输入框上方，并触发与任务相同的通知和提醒
  - 题型：支持单选、多选和自由填写；每个选项独占一行，可根据问题补充影响与取舍说明
  - 题量：单轮支持 1 至 3 个相互独立的问题；复杂决策允许多轮问卷，但不得为次要选择频繁打断用户
  - 操作：每题分别提供“下一步/确认”和“取消”，单题取消不得影响同轮其他问题
  - 取消处理：用户取消某题后，询问其是否有其他想法或希望调整方案；不得直接中止整个任务
  - 上下文：问题、选项、用户答案及取消结果均作为结构化消息写入消息流，并进入后续模型上下文
  - 续作：一轮问卷结束后，Agent 立即依据结果继续执行，直到任务完成或遇到新的关键抉择点
  - 权限关系：问卷与文件/命令权限相互独立，在计划、接受编辑及其他权限模式下均可触发
  - 设计参考：采用小型、可组合的“grilling”式澄清机制，只解决当前决策分支，不把整个任务改造成冗长访谈

## i18n 待办

- [ ] **server.py `_execute_subagent_tool`** — 子 Agent 工具结果硬编码中文
- [ ] **i18n 架构统一** — HTML `data-i18n` / JS `t()` / server `_serverErrorMap` 三层合并
- [ ] **LANG 与 I18N 字典合并** — 两套字典去重

---

_上次更新：2026-07-14_
