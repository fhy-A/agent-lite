# Agent Lite 全面测试题库

## 1. 工具编排（多工具组合）

### 1.1 文件定位 + 读取 + 分析
> agent-lite 项目一共有多少个 JS 文件？分别列出行数和功能

### 1.2 跨文件搜索 + 提取
> 找到 app.js 里所有使用 "fetch" 的地方，统计通过哪个代理路径发出的请求最多

### 1.3 定位 + 修改 + 验证
> styles.css 里有多少处 font-size: 12px？如果没有，报告"没有"。如果有，只改第一处为 13px，然后 git diff 确认只改了一处

## 2. 代码理解与生成

### 2.1 逻辑解释
> server.py 里的 proxy 函数做了哪些安全处理？每项列一行

### 2.2 代码生成
> 写一个 Python 脚本，读取 data/sessions/ 下体积最大的 3 个 session 文件，输出文件名和大小

### 2.3 Bug 诊断
> 如果一个代码文件保存后内容变成空白，从 server.py 的代码逻辑来看，可能是什么原因？

## 3. 文档操作

### 3.1 生成报告
> 把 VERSION、release-notes.md、README.md 的内容汇总成一份 HTML 格式的版本摘要，保存到桌面

### 3.2 格式转换
> 把 release-notes.md 转成一份 PPT（用 python-pptx），每段一个 slide

## 4. 数据处理

### 4.1 统计查询
> 计算 app.js 中每个函数的行数，输出前三长的函数名、行数和复杂度评估

### 4.2 日志分析
> 如果 server.py 有运行日志，找出返回码不是 200 的请求统计

## 5. 图片 + 文本混合

### 5.1 图片描述
> [粘贴 3 张不同的截图] 描述每张图的内容，分别用一句话

### 5.2 图片 + 代码
> [粘贴报错截图] 这个报错是什么意思？帮我找到 app.js 里对应的代码位置

## 6. 长对话上下文

### 6.1 连续追问（5 轮）
> ① agent-lite 有几个核心模块？各是什么？
> ② server.py 的那个模块主要负责什么？
> ③ 它怎么处理图片请求？
> ④ 如果我想加一个新的 API 端点，需要改 server.py 的哪些地方？
> ⑤ 好，那请在 server.py 里加一个 /api/ping 端点，返回 {"pong": true}——但用 propose_edit 生成 diff 给我看一下，不要直接写入

### 6.2 跨轮记忆
> ① 帮我记住：我最喜欢的编程语言是 Python
> ② [过两轮对话后] 我之前说我最喜欢的编程语言是什么？

## 7. 错误恢复

### 7.1 修复错误
> [故意给一个不存在的命令] 执行：删除一个不存在的文件——看它怎么报错和恢复

### 7.2 超长输出
> 读取整个 app.js，统计每个字母的出现频率

## 8. 边界测试

### 8.1 大文件
> 搜索 app.js 里的所有函数定义，列出函数名、起止行号

### 8.2 特殊字符
> 新建一个文件，文件名为「测试-2024」，内容包含 emoji 和中文，然后读出来确认

### 8.3 并发请求
> 同时读取 server.py 和 app.js 的第一行，告诉我分别是哪个语言的什么声明
